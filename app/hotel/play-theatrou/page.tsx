/**
 * Next.js App Router would use `page.tsx` here. Expo Router uses `index.tsx` for the same URL
 * (`/hotel/play-theatrou`). This file is a marker only — no default export (avoids a second route
 * at `/hotel/play-theatrou/page`). The screen lives in `./index.tsx`.
 */
export {};
